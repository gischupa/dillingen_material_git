version: '3'
services:
 java:
   build:
     context: .
   image: java-git:1.0
   volumes:
     - ./ergebnis:/ergebnis
   networks:
      - gitlab_default

networks:
  gitlab_default:
    external: true

